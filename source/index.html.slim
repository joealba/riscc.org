---
title: Rhode Island Service Coordinator Collaborative
---

section.homepage-story
  .container
    .row
      .story-content
        h1 = data.pages.home.hero.title
        = markdown data.pages.home.story

/ section.homepage-events.events
/   .container
/     h2
/       | Upcoming events
/     .row
/       - data.events.sort {|a,b| b[1].event_date <=> a[1].event_date}.first(3).each do | event |

/         //to get the actual data:
/         = partial 'partials/events/event', locals: {event: event[1]}

/ section.homepage-news.news
/   .container
/     h2
/       | News
/     .news-items
/       //get the first two most recent posts
/       - blog.articles.first(2).each do | article |
/         = partial 'partials/news/news_item', locals: {article: article}